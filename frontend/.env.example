# Copy this file to .env.local and fill in real values. Never commit .env.local.

# Base URL of the NestJS backend API. Server components (lib/auth-server.ts)
# call this directly; client components go through the same-origin rewrite
# in next.config.ts instead, which also points at this value.
NEXT_PUBLIC_API_URL="http://localhost:4000"
